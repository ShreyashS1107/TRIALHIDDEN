'use client';

import React, { useState, useEffect } from 'react';
import Link from 'next/link';
import { useRouter } from 'next/navigation';
import Navbar from '@/components/layout/Navbar';
import { CustomProjectInput, CustomProjectPredictionResponse } from '@/lib/api/predict';
import { formatIndianNumber } from '@/lib/utils/format';
import {
  ArrowLeft,
  Sparkles,
  Building2,
  MapPin,
  Calendar,
  FileCheck,
  Play,
  RotateCcw
} from 'lucide-react';

import { RiskOverview } from '@/components/projects/custom/analytics/RiskOverview';
import { CostAnalytics } from '@/components/projects/custom/analytics/CostAnalytics';
import { ScheduleTimeline } from '@/components/projects/custom/analytics/ScheduleTimeline';
import { ExecutionEfficiency } from '@/components/projects/custom/analytics/ExecutionEfficiency';
import { AssessmentState } from '@/components/projects/custom/analytics/AssessmentState';
import { RiskSignals } from '@/components/projects/custom/analytics/RiskSignals';
import { DecisionSupport } from '@/components/projects/custom/analytics/DecisionSupport';
import { ModelTransparency } from '@/components/projects/custom/analytics/ModelTransparency';

const DEFAULT_BENCHMARK_INPUT: CustomProjectInput = {
  project_name: 'Bengaluru High-Speed Airport Metro Link',
  project_id: 'BMRCL-AIRPORT-LINK-01',
  agency: 'Ministry of Railways [MOR]',
  state: 'Karnataka',
  original_cost_crore: 5600.0,
  revised_cost_crore: 6450.0,
  approval_start_date: '2023-01',
  original_completion_date: '2026-12',
  revised_completion_date: '2027-09',
  physical_progress_percent: 34.0,
  is_completed: false,
  cumulative_expenditure_crore: 2200.0,
  sector: 'Urban Mass Transit'
};

// Authoritative benchmark prediction output directly generated by the trained ML model pipeline
const DEFAULT_BENCHMARK_ML_PREDICTION: CustomProjectPredictionResponse = {
  project_id: 'BMRCL-AIRPORT-LINK-01',
  project_name: 'Bengaluru High-Speed Airport Metro Link',
  agency: 'Ministry of Railways [MOR]',
  state: 'Karnataka',
  original_cost_crore: 5600.0,
  revised_cost_crore: 6450.0,
  approval_start_date: '2023-01',
  original_completion_date: '2026-12',
  revised_completion_date: '2027-09',
  physical_progress_percent: 34.0,
  cumulative_expenditure_crore: 2200.0,
  schedule_delay_risk: 0.4257,
  cost_overrun_risk: 0.8689,
  schedule_revision_risk: 0.0,
  selected_integrated_risk: 0.517,
  risk_band: 'MEDIUM',
  dominant_component: 'Cost Overrun',
  predicted_cost_crore: 7346.7,
  predicted_delay_months: 15,
  predicted_completion_date: '2028-12',
  model_confidence: null,
  recommended_intervention: 'Establish bi-weekly physical milestone monitoring; audit contractor equipment mobilization and vendor payment liquidity.',
  risk_drivers: [
    {
      id: 'cost_escalation',
      name: 'Cost Escalation & Budget Expansion',
      impact_pct: 87,
      impact_level: 'High impact',
      description: 'Approved capital allocation escalated by +15.2% over original sanction.'
    },
    {
      id: 'schedule_pressure',
      name: 'Schedule Delay & Milestone Slippage',
      impact_pct: 43,
      impact_level: 'Medium impact',
      description: '9 months milestone slippage recorded; model identifies critical timeline compression.'
    },
    {
      id: 'progress_drag',
      name: 'Physical Execution Velocity Drag',
      impact_pct: 38,
      impact_level: 'High impact',
      description: 'Remaining 66.0% physical scope requires sustained contractor deployment to avoid stagnation.'
    },
    {
      id: 'revision_risk',
      name: 'Administrative Extension Vulnerability',
      impact_pct: 0,
      impact_level: 'Low impact',
      description: 'Model assesses likelihood of administrative date re-baselining or contractual extension request.'
    }
  ],
  execution_stress_index: 0.608,
  esi_tier: 'Moderate Stress',
  is_ml_model: true,
  model_version: 'PAIMANA-ML-RF02-Calibrated-v2.0',
  model_metadata: {
    primary_model: 'rf02_calibrated.pkl (RandomForest + Platt Sigmoid)',
    cost_model: 'cost_overrun_state_3m/model.pkl (Balanced RandomForest)',
    revision_model: 'schedule_revision_3m/model.pkl (Regularized Logistic Regression)',
    scoring_formula: 'Candidate B (0.50 Sched + 0.35 Cost + 0.15 Rev)',
    features_used: 75,
    holdout_brier_score: 0.0389,
    holdout_roc_auc: 0.9723
  },
  timestamp: '2026-09-10T05:15:40.008051Z'
};

export default function CustomProjectResultPage() {
  const router = useRouter();

  const [inputData, setInputData] = useState<CustomProjectInput>(DEFAULT_BENCHMARK_INPUT);
  const [prediction, setPrediction] = useState<CustomProjectPredictionResponse>(DEFAULT_BENCHMARK_ML_PREDICTION);

  useEffect(() => {
    if (typeof window !== 'undefined') {
      try {
        const stored = sessionStorage.getItem('paimana_custom_assessment') || localStorage.getItem('paimana_custom_assessment');
        if (stored) {
          const parsed = JSON.parse(stored);
          if (parsed?.input && parsed?.result) {
            setInputData(parsed.input);
            setPrediction(parsed.result);
          }
        }
      } catch (e) {
        console.error('Error loading stored custom project assessment:', e);
      }
    }
  }, []);

  return (
    <main className="min-h-screen bg-slate-50 dark:bg-[#040d13] text-slate-900 dark:text-slate-100 flex flex-col justify-between">
      <Navbar />

      {/* TOP SECTION: IDENTIFICATION & DEMARCATION */}
      <div className="pt-24 border-b border-slate-200 dark:border-white/10 bg-slate-50/60 dark:bg-[#03090e]/70 backdrop-blur-md pb-8">
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 space-y-4">
          {/* Breadcrumb & Provenance Tags */}
          <div className="flex flex-wrap items-center justify-between gap-3">
            <Link
              href="/custom-project"
              className="inline-flex items-center gap-2 text-xs font-mono font-semibold tracking-wider text-slate-600 dark:text-slate-400 hover:text-cyan-600 dark:hover:text-cyan-400 transition-colors uppercase py-1 px-2.5 rounded-lg border border-slate-200 dark:border-white/10 hover:border-cyan-500/30 bg-white dark:bg-white/[0.02]"
            >
              <ArrowLeft className="w-3.5 h-3.5" />
              <span>Back to Assessment Intake</span>
            </Link>

            <div className="flex flex-wrap items-center gap-2">
              <span className="hidden sm:inline-flex items-center text-[10px] font-mono text-slate-500 dark:text-slate-400 px-2.5 py-0.5 rounded bg-slate-100 dark:bg-white/5 border border-slate-200 dark:border-white/10">
                Prediction generated from PAIMANA trained risk models
              </span>
              <span className="inline-flex items-center gap-1.5 px-3 py-1 rounded-full bg-purple-500/10 border border-purple-500/30 text-purple-600 dark:text-purple-400 text-xs font-mono font-bold tracking-wider uppercase animate-pulse">
                <Sparkles className="w-3.5 h-3.5" />
                MODEL PREDICTION
              </span>
              <span className="inline-flex items-center gap-1.5 px-3 py-1 rounded-full bg-cyan-500/10 border border-cyan-500/30 text-cyan-600 dark:text-cyan-400 text-xs font-mono font-bold tracking-wider uppercase">
                <FileCheck className="w-3.5 h-3.5" />
                ML ASSESSMENT
              </span>
            </div>
          </div>

          {/* Asset Identity */}
          <div className="space-y-2">
            <div className="flex items-center gap-2">
              <span className="text-[11px] font-mono font-bold tracking-widest px-2.5 py-0.5 rounded bg-cyan-500/10 text-cyan-600 dark:text-cyan-400 border border-cyan-500/20 uppercase">
                NEW PROJECT ASSESSMENT
              </span>
              <span className="text-[11px] font-mono px-2 py-0.5 rounded bg-slate-200 dark:bg-white/5 text-slate-700 dark:text-slate-300 border border-slate-300 dark:border-white/10">
                REF: {prediction.project_id}
              </span>
            </div>

            <h1 className="text-2xl sm:text-3xl lg:text-4xl font-extrabold tracking-tight text-slate-900 dark:text-white leading-snug">
              {prediction.project_name}
            </h1>

            <div className="flex flex-wrap items-center gap-4 text-xs text-slate-600 dark:text-slate-400 pt-1">
              <div className="flex items-center gap-1.5 font-medium">
                <Building2 className="w-4 h-4 text-cyan-500 flex-shrink-0" />
                <span>{prediction.agency}</span>
              </div>
              <span className="text-slate-300 dark:text-white/20">•</span>
              <div className="flex items-center gap-1.5 font-medium">
                <MapPin className="w-4 h-4 text-cyan-500 flex-shrink-0" />
                <span>{prediction.state}</span>
              </div>
              <span className="text-slate-300 dark:text-white/20">•</span>
              <div className="flex items-center gap-1.5 font-mono text-[11px]">
                <Calendar className="w-3.5 h-3.5 text-slate-400" />
                <span>Approved: {inputData.approval_start_date}</span>
              </div>
            </div>
          </div>
        </div>
      </div>

      {/* MAIN DASHBOARD CONTENT */}
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-10 space-y-10">
        {/* SECTION A: OVERALL RISK HERO */}
        <RiskOverview prediction={prediction} />

        {/* SECTION B: COST ANALYSIS & FINANCIAL EXPOSURE */}
        <CostAnalytics input={inputData} prediction={prediction} />

        {/* SECTION C: DELIVERY SCHEDULE INTELLIGENCE */}
        <ScheduleTimeline input={inputData} prediction={prediction} />

        {/* SECTION D: COST VS PHYSICAL PROGRESS (EXECUTION EFFICIENCY SIGNAL) */}
        <ExecutionEfficiency input={inputData} prediction={prediction} />

        {/* SECTION E: CURRENT MODEL ASSESSMENT STATE */}
        <AssessmentState prediction={prediction} />

        {/* SECTION F: RANKED MODEL-DERIVED RISK SIGNALS (EXPLAINABILITY) */}
        <RiskSignals prediction={prediction} />

        {/* SECTION G: WHAT SHOULD HAPPEN NEXT? (DECISION SUPPORT) */}
        <DecisionSupport prediction={prediction} />

        {/* SECTION H: MODEL TRANSPARENCY & ARCHITECTURAL SPECS */}
        <ModelTransparency prediction={prediction} />

        {/* FINAL ACTIONS & CTAS */}
        <section className="p-6 sm:p-7 rounded-2xl bg-white dark:bg-[#06131c]/80 border border-slate-200 dark:border-white/10 shadow-sm flex flex-wrap items-center justify-between gap-4">
          <div className="flex flex-wrap items-center gap-3">
            <Link
              href={`/simulation?project=${encodeURIComponent(prediction.project_id)}`}
              className="inline-flex items-center gap-2 px-5 py-3 rounded-xl bg-cyan-500 hover:bg-cyan-400 text-slate-950 text-xs font-mono font-bold tracking-wider shadow-md shadow-cyan-500/20 transition-all duration-150"
            >
              <Play className="w-4 h-4 fill-current" />
              <span>SIMULATE ANOTHER SCENARIO</span>
            </Link>

            <Link
              href="/custom-project"
              className="inline-flex items-center gap-2 px-5 py-3 rounded-xl bg-slate-100 dark:bg-white/5 hover:bg-slate-200 dark:hover:bg-white/10 border border-slate-300 dark:border-white/10 text-slate-800 dark:text-slate-200 text-xs font-mono font-bold tracking-wider transition-colors"
            >
              <RotateCcw className="w-4 h-4 text-cyan-500" />
              <span>ASSESS ANOTHER PROJECT</span>
            </Link>
          </div>

          <div>
            <Link
              href="/"
              className="inline-flex items-center gap-2 px-5 py-3 rounded-xl bg-slate-100 dark:bg-white/5 hover:bg-slate-200 dark:hover:bg-white/10 border border-slate-300 dark:border-white/10 text-slate-800 dark:text-slate-200 text-xs font-mono font-bold tracking-wider transition-colors"
            >
              <ArrowLeft className="w-4 h-4" />
              <span>RETURN TO COMMAND CENTER</span>
            </Link>
          </div>
        </section>
      </div>

      {/* FOOTER */}
      <footer className="border-t border-slate-200 dark:border-white/10 py-8 bg-slate-100 dark:bg-[#03090e] text-center text-xs font-mono text-slate-500 space-y-1">
        <p>PAIMANA • Project Intelligence Results • MoSPI National Infrastructure Surveillance</p>
        <p className="text-[11px] text-slate-400 dark:text-slate-500">
          Evaluated via Authoritative RF_02 Holdout Calibrated ML Inference Engine
        </p>
      </footer>
    </main>
  );
}
